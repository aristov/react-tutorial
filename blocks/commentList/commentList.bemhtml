block('commentList')(
    js()(true),
    content()(function() {
        return (this.ctx.data || []).map(function(comment) {
            return {
                block: 'comment',
                author: comment.author,
                text: comment.text
            }
        })
    })
)